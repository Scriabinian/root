@컬렉션 프레임워크

프로그램 실행 시 메모리에 자료를 유지하고 관리하며 제약의 단점을 보완할 수 있다.
컬렉션 프레임워크는 자료구조(스택/큐/트리) 미리 구현할 수 있는 인터페이스 모임.

@리스트/어레이리스트/링크드리스트
@셋/해시셋/링크드해시셋
@맵

@상속,객체배열,스트림

1.자바 프로젝트(SBS_Labs)를 생성한다.
2.Student 클래스와 GIS클래스를 각각 생성하고 멤버 변수와 멤버 함수를 생성.
3.프로그램 진입점인 main 함수(DisplayInfo 클래스)에서  BufferReader 이용해서 2학년. csv 파일을 불러온다.
4.파일을 전체적으로 읽어서 데이터 개수가 몇 개 인지 파악한다.
5.GIS 배열([])의 객체를 학생수 만큼 생성한당.
6. 파일을 다시 읽어서 GIS 배열([])객체 요소에 학과, 학년, 학번, 성명, 거주지역, 이메일을 할당한다.(scanner로 자동으로 읽어낸다.)
7.Stream 을 닫음.
8. GIS배열([])의 객체에 들어있는 값은 for-each문장과 display멤버함수를 이용해서 출력한다.
